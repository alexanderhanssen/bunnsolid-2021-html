{
  "name": "my-project",
  "scripts": {
    "build": "parcel build index.html",
    "start": "parcel serve index.html",
    "serve": "npx browser-sync dist"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "parcel": "next"
  }
}
